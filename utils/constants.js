const PORT = 3000;
const NODE_ENV = 'production';
const JWT_SECRET = 'some-secret-key';
const DB_ADDRESS = 'mongodb://127.0.0.1:27017/bitfilmsdb';

module.exports = {
  PORT, NODE_ENV, JWT_SECRET, DB_ADDRESS,
};
